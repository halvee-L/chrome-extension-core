require([
  "../util/MessageService",
  "../util/Store",
  "../lib/jquery-3.4.1.min.js"
], function(MessageService, Store) {
  let msserver = new MessageService();
  let store = new Store();
  $(function() {});
});
